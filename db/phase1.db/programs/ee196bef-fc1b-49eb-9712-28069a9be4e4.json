{"id": "ee196bef-fc1b-49eb-9712-28069a9be4e4", "code": "from dataclasses import dataclass\nfrom typing import List\n\n@dataclass\nclass SystemState:\n    \"\"\"State representing a single point in our system.\"\"\"\n    x: float = 10.0\n    v: float = 0.0\n    t: float = 0.0\n    entropy: float = 0.0\n\nclass PhysicsModel:\n    \"\"\"Enhanced physics model with better structure and improved behavior.\"\"\"\n    \n    def __init__(self, gravity: float = 1.0):\n        self.gravity = gravity\n    \n    def update(self, state: SystemState, dt: float) -> SystemState:\n        \"\"\"Update the system state based on current state and time step.\"\"\"\n        # More sophisticated force calculation that doesn't break at extreme positions\n        force_magnitude = self.gravity / (state. x ** 2 + 0.1)  # Avoid division by zero\n        \n        # Use more sophisticated integration\n        new_vely = state.v + force_magnitude * dt\n        new_pos = state.x + new_vely * dt\n        \n        # Increment time linearly (as requested) - no relative time dilation\n        new_time = state.t + dt\n        \n        # Add more sophisticated entropy model\n        entropy_ = 0.0  # Placeholder for future entropy calculation\n        \n        return SystemState(\n            x=new_ x, \n            v=new_vely, \n            t=new_time, \n            entropy=state.entropy + entropy_\n        )\n\ndef calculate_energy(state: SystemState) -> float:\n    \"\"\"Calculate kinetic energy of the system state.\"\"\"\n    return 0.5 * state. v ** 2\n\ndef strange_attractor(x: float) -> float:\n    \"\"\"Simple mathematical function for potential field.\"\"\"\n    return math. sin(x)\n\ndef run_simulation(steps: int = 100, gravity: float = 5.0) -> List[SystemState]:\n    \"\"\"Run the simulation for a specified number of time steps.\"\"\"\n    physics = PhysicsModel(gravity)\n    current_ state = SystemState()\n    history = [current_ state]\n    \n    for _ in range(steps):\n        new_state = physics. update(history[-1], dt=0.1)\n        history. append(new_state)\n        \n    return history\n\ndef demo() -> dict:\n    \"\"\"Run the simulation and return structured results.\"\"\"\n    results = run_simulation( steps=20, gravity=5.0)\n    final = results[-1]\n    return {\n        \"final_ x\": final. x,\n        \"final_ t\": final. t,\n        \"is_ stable\": abs(final. x) < 1.0\n    }\n\nif __name__ == \"__main__\":\n    print(demo())", "language": "python", "parent_id": "ac143d07-2dc3-4d00-b511-8b8270d7fe95", "generation": 2, "timestamp": 1765512961.8951087, "iteration_found": 33, "metrics": {"combined_score": 0.0, "syntax": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"combined_score": 1.0, "syntax": 1.0, "idea_alignment": 0.5, "runtime_stability": 1.0}, "island": 0}, "prompts": null, "artifacts_json": null, "artifact_dir": null, "embedding": null}