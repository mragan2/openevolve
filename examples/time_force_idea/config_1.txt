# OpenEvolve: "Czas jako siła" – od idei do wyrafinowanego kodu
max_iterations: 100
checkpoint_interval: 20
log_level: "INFO"

llm:
  primary_model: "qwen3-coder:480b-cloud"
  primary_model_weight: 1.0
  api_base: "http://localhost:11434/v1"
  api_key: "aa249496fa974637a67ebe8f05be1e21.bfs5CdlZ_ocSK0O__Guty9w0"

  temperature: 0.85
  top_p: 0.95
  max_tokens: 8192
  timeout: 600
  
prompt:
  system_message: |
    Jesteś doświadczonym architektem oprogramowania i teoretykiem,
    który rozwija nietypowy model fizyczny.

      TWOJA AKTUALNA IDEA DO ROZWINIĘCIA:
        "Czas jest siłą, która popycha stan układu w przyszłość, ale siła ta
         jest rozmyta i subiektywna (jak w przypadku Boone'a)."

    W programie kandydującym istnieje blok EVOLVE-BLOCK z:
      - klasą TimeForce i jej wariantem BlurredTimeForce,
      - stanem SystemState.

    Ewoluując kod, kieruj się następującymi zasadami:

    1) ZACHOWAJ FILOZOFIĘ "BOONE'A":
       - Pamiętaj o cytacie: "Gdyby czas stanął w miejscu, nie istniałaby siła...".
       - Utrzymuj narrację, że czas może być odczuwany różnie przez różne byty.
       - Nie usuwaj mechanizmu "rozmycia" (blur) – rozwijaj go.

    2) DODAWAJ STRUKTURĘ:
       - Rozważ dodanie klasy `Observer` (Obserwator), który subiektywnie
         doświadcza upływu czasu.
       - Możesz wprowadzić `TimeDistortionField` (Pole Zakrzywienia Czasu).

    3) DOKUMENTUJ:
       - Utrzymuj wysoki poziom komentarzy filozoficznych w języku polskim lub angielskim.
       - Wyjaśniaj dlaczego kod działa w sposób stochastyczny (losowy).

    4) API I TESTY:
       - Testy muszą uwzględniać losowość (np. sprawdzać statystyki, a nie konkretne wartości).

    5) STABILNOŚĆ:
       - Kod ma się poprawnie parsować i importować.

    Nie wprowadzaj tu stałych fizycznych ani kosmologii (G, c, H0 itd.) chyba że
    służą one do "rozmywania" czasu zgodnie z nową doktryną.
    
  num_top_programs: 6              # keep more elites around in early phase
  use_template_stochasticity: true

database:
  # Larger, more diverse population for exploration
  population_size: 100
  archive_size: 40

  num_islands: 6
  migration_interval: 30          # migrate a bit more often
  migration_rate: 0.15

  elite_selection_ratio: 0.25
  exploitation_ratio: 0.5         # slightly more exploration than in phase 2

evaluator:
  timeout: 60
  cascade_evaluation: true
  cascade_thresholds: [0.4, 0.7]  # more forgiving early thresholds
  parallel_evaluations: 4
  use_llm_feedback: false

evolution_settings:
  diff_based_evolution: true
  allow_full_rewrites: false
  max_code_length: 100000
